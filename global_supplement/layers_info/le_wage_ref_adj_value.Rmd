See **`r layer_meta$name[layer_meta$layer == 'le_wage_sector_year']`** layer for information about data and methods.  

The adjustment for each sector/region is 1, all adjustments to wage data used in the livelihoods subgoal were made directly to the wage data layers.
