See **`r layer_meta$name[layer_meta$layer == 'le_wage_sector_year']`** layer for information about data and methods.  

This layer describes the wage data (from the **`r layer_meta$name[layer_meta$layer == 'le_wage_sector_year']`** layer) for the earliest year of data for each sector/region.


