See **`r layer_meta$name[layer_meta$layer == 'le_rev_sector_year']`** layer for more information about data and methods.  

GDP during the reference year (i.e., earliest year of data) of each sector/region (World Bank). Revenue values for each sector/region were adjusted by dividing by GDP in the corresponding year.
