See **`r layer_meta$name[layer_meta$layer == 'le_wage_sector_year']`** layer for informationa about data and methods.  

The adjustment for each sector/region is 1, all adjustments were made in directly to the wage data layers.
